<div class="card">    
  <div class="card-content">
   
      <div class="row">
        <%= simple_form_for :other, url: url_for(action: scan_action, :controller => 'scanpatient'), :method => scan_method do |f| %>
        <%= f.error_notification %>
        <div class="row">
            <div class="form-inputs">
            <%= f.input :patient_account_num, required: true, label: "Scan the patient's wristband barcode",label_html: { class: 'active' } %>
            </div>
        </div>
        <div class="row">
            <div class="waves-effect waves-light btn" >
            <%= f.button :submit, 'Submit'%>
            </div>
        </div>
    
        
        <% end %>
    </div>
  </div>
</div>
  
  <div class="row">
    <div class="form-actions col s7">
    </div>
  </div>